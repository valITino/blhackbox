# ── LLM Provider (optional — only needed for CLI usage) ───────────────
# Not required when using Claude Code or Claude Desktop (they provide
# their own authentication). Only set these if you use the blhackbox CLI
# with the Anthropic or OpenAI APIs directly.
#
# Claude — get key at console.anthropic.com
# ANTHROPIC_API_KEY=
#
# OpenAI — get key at platform.openai.com
# OPENAI_API_KEY=

# ── Ollama ────────────────────────────────────────────────────────────
OLLAMA_MODEL=llama3.3

# ── MCP Gateway ───────────────────────────────────────────────────────
MCP_GATEWAY_PORT=8080

# ── Neo4j (only needed with --profile neo4j) ─────────────────────────
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme-min-8-chars

# Neo4j Aura alternative (cloud):
# NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
