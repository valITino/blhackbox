# Version control
.git
.gitignore
.gitmodules

# Environment and secrets
.env
.env.*
!.env.example

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# Testing and CI
tests/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Docker (avoid recursive build context)
docker-compose.yml
Dockerfile

# Documentation (root-level only — do NOT use a bare *.md glob here;
# it would strip blhackbox/prompts/**/*.md which are required at runtime)
/README.md
/DOCKER.md
/LICENSE

# Results and data (may contain sensitive scan data)
results/
wordlists/
neo4j_data/
neo4j_logs/

# GitHub workflows
.github/

# Makefile (not needed in container)
Makefile

# Scripts (not needed in production image)
scripts/

# hexstrike submodule – only needed for hexstrike.Dockerfile build
# (not excluded; the hexstrike service needs these files in its build context)
