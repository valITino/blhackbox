# Core - pinned for reproducibility and supply-chain safety
click==8.1.8
httpx==0.28.1
pydantic>=2.11.0
pydantic-settings>=2.10.1
rich==13.9.4

# Neo4j (optional — used with --profile neo4j)
neo4j==5.27.0

# Reporting
weasyprint==68.1
jinja2==3.1.6
matplotlib==3.10.0

# MCP (FastMCP for automatic tool schema generation)
mcp>=1.23.0

# Pygments — transitive dep of rich; pinned to pick up CVE-2026-4539 fix
# when available.  CVE is low-severity local ReDoS in AdlLexer (CVSS 3.3),
# no upstream patch exists yet.  See: https://nvd.nist.gov/vuln/detail/CVE-2026-4539
pygments>=2.19.2

# Utilities
tenacity==9.0.0
python-dotenv==1.2.2
aiofiles==24.1.0
